These scripts are provided to simplify updating your server code and launching it. Feel free to modify them.

update.sh: creates a servers.txt file for each virtual machine, copies your code do the virtual machine and compiles it.

launch.sh: launches the program "server" in each virtual machine

servers.sh: contains the list of server IP addresses

do.sh: execute a command on all the servers
